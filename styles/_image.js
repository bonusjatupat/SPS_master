import { StyleSheet, Platform } from 'react-native';

export default StyleSheet.create({
    
    carIcon:{
        marginTop:15,
        width: 60,
        height: 60,
        position:'absolute',
        alignSelf:'flex-end',
        marginTop:30
    },

})
