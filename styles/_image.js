import { StyleSheet, Platform } from 'react-native';

export default StyleSheet.create({
    
    carIcon:{
        width: '80%',
        height: '100%',
        marginTop:'20%',
        alignSelf:'center',
    },

})
